import React from 'react';
import { View, Text, ScrollView } from 'react-native';
//import { VictoryChart, VictoryLine, VictoryTheme } from 'victory-native';

export default function VitalsChartScreen({ route }) {
//   const { vitals } = route.params;

//   const data = vitals.map(v => ({
//     x: v.time.split(" ")[1],  // время
//     y: v.temp                 // температура
//   }));

//   return (
//     <ScrollView style={{ padding: 20 }}>
//       <Text style={{ fontSize: 22, fontWeight: "700", marginBottom: 20 }}>
//         График температуры
//       </Text>

//       <VictoryChart theme={VictoryTheme.material}>
//         <VictoryLine
//           data={data}
//           interpolation="natural"
//           style={{
//             data: { stroke: "#007aff", strokeWidth: 3 }
//           }}
//         />
//       </VictoryChart>
//     </ScrollView>
//   );
}
